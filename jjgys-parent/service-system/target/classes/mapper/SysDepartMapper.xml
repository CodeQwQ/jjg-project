<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="glgc.jjgys.system.mapper.SysDepartMapper">

    <resultMap type="glgc.jjgys.model.system.SysDept" id="SysDeptResult">
        <id     property="id"     column="id"     />
        <result property="name"   column="name"   />
        <result property="parentId"  column="parent_id"   />
        <result property="treePath"   column="tree_path"   />
        <result property="sortValue"   column="sort_value"   />
        <result property="leader"     column="leader"      />
        <result property="phone"      column="phone"       />
        <result property="status"      column="status"       />
        <result property="createTime" column="create_time" />
        <result property="companyId"   column="company_id"   />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectDeptVo">
        select d.id, d.name, d.parent_id, d.tree_path, d.sort_value, d.leader, d.phone, d.status, d.create_time,d.company_id,d.update_time
        from sys_dept d
    </sql>

    <select id="checkDeptNameUnique" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where name=#{name}
    </select>

    <select id="hasChildByDeptId" parameterType="Long" resultType="int">
        select count(1) from sys_dept
        where parent_id = #{id}
    </select>

    <select id="checkDeptExistUser" parameterType="Long" resultType="int">
        select count(1) from sys_user where dept_id = #{id}
    </select>



</mapper>